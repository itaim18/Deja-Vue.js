import React from "react";

const DownloadLink = () => {
  return (
    <div id="download-link" className="download-link">
      <div className="container active reveal fade-left">
        <h1>start now.</h1>
        <button className="btn download-btn">download now</button>
      </div>
    </div>
  );
};

export default DownloadLink;
